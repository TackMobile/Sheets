buildscript {
  repositories { mavenCentral() }
  dependencies { classpath 'com.android.tools.build:gradle:0.5.+' }
}

allprojects {
  group = 'com.tackmobile.sheets'

  repositories {
    mavenCentral()
  }
}

apply plugin: 'android-reporting'
